source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in olivander.gemspec.
gemspec

gem "pg"

gem "sprockets-rails"

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"

gem 'chartkick'
gem 'effective_datatables'
gem 'haml-rails', '~> 2.0'
gem 'simple_form'
gem 'view_component'

group :development do
  gem 'pry-byebug'
  gem 'pry-rails'
end
